# Modelo das variáveis de ambiente necessárias (ESTE vai para o Git).
# Cada membro copia este ficheiro para .env e preenche com os seus valores:
#   cp .env.example .env
# Gerar uma chave segura:  openssl rand -hex 64
JWT_SECRET=cole_aqui_a_sua_chave_gerada_com_openssl
